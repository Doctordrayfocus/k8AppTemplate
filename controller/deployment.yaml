apiVersion: apps/v1
kind: Deployment
metadata:
  name: apptemplate-controller
spec:
  replicas: 1
  serviceAccountName: app-template
  selector:
    matchLabels:
      app: apptemplate-controller
  template:
    metadata:
      labels:
        app: apptemplate-controller
    spec:
      containers:
        - name: apptemplate
          image: drayfocus/apptemplate-controller:0.1
          ports:
            - containerPort: 8080
          imagePullPolicy: Always